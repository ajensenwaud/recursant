## Helm values overlay for cluster-2 in a multi-cluster active-active deployment.
## Usage:
##   helm upgrade --install recursant k8s/charts/recursant \
##     -f k8s/charts/recursant/values-cluster-2.yaml \
##     --set multiCluster.remoteDbHost=<cluster-1-node-ip> \
##     --set multiCluster.remoteRegistryUrl=http://<cluster-1-node-ip>:30500 \
##     --kube-context kind-recursant-2

multiCluster:
  enabled: true
  clusterId: "cluster-2"
  # Override these at install time with --set
  remoteRegistryUrl: "http://REPLACE_ME:30500"
  remoteDbHost: "REPLACE_ME"
  remoteDbPort: 30432

db:
  replication:
    enabled: true
    clusterName: "cluster-2"
    remoteHost: "REPLACE_ME"
    remotePort: 30432
