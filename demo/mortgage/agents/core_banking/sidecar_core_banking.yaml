recursant:
  sidecar:
    port: 9914
    a2a_port: 8454
    agent_port: 5024
    registry_url: "http://registry:5000"
    agent_card_path: "./agent_card.yaml"
    log_level: "info"
    interceptors:
      authentication:
        enabled: true
        schemes: ["mtls", "api_key"]
      authorisation:
        enabled: true
        default_action: deny
      audit:
        enabled: true
    tls:
      cert_path: "/certs/sidecar-core-banking.pem"
      key_path: "/certs/sidecar-core-banking-key.pem"
      ca_path: "/certs/ca.pem"
