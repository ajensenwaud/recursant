FROM python:3.11-slim

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

# Install the mesh package
COPY pyproject.toml .
COPY runtime/ runtime/

RUN pip install --no-cache-dir -e .

EXPOSE 8080

CMD ["python", "-m", "runtime.gateway"]
